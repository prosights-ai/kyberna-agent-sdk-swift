Recorded, language-neutral fixtures: Claude Code stream-json conversations, expected CLI argument lists, SSE byte streams per provider, and session-store format samples. Consumed by the Swift tests here and, later, by the Python and .NET ports as a submodule. Every file must be free of credentials and of real channel messages; record against a pinned Claude Code version and name the version in the file.
